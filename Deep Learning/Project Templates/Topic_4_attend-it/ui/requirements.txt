# UI tier (deployed on Streamlit Community Cloud)
# No torch, no model code here -- a thin client on purpose.
# Attention weights arrive as plain numbers from the API; the UI just plots them.
streamlit==1.38.0
requests==2.32.3
pandas==2.2.2
supabase==2.7.4
