# Default hyperparameters for a training run.
# The API accepts these from the request body; this file documents sane starting
# values and gives later products (with CLI training) a config to load.
lr: 0.005
batch_size: 64
epochs: 20
test_size: 0.2
